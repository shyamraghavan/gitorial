extends base

block nav
	+navbar("home")

block head
	if (user)
		title= 'gitorial | ' + user.name.givenName + '\'s profile'
	else
		title gitorial | join!

block body
	if (user)
		p Hello World
	else
		link(rel="stylesheet" type="text/css" href="/css/home.css")

		div.logo
			img(src="/images/gitorial_header.png")

		div.tutorials


block script
